import Title from '../components/Title';
import './ContactUs.css'

const Home = () => (
  <div className="page-content">
    <Title text="Contact Us"/>
    <h1 className="title-text">Contact Us</h1>
    <p>We won't answer though...</p>
  </div>
);

export default Home;
